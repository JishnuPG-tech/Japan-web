{
  "name": "komorebi-japan",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.mjs",
    "start": "node server.mjs",
    "check": "node --check app.js && node --check scene.js && node --check world.js && node --check server.mjs",
    "test": "node --test app.test.mjs"
  },
  "engines": { "node": ">=20" }
}
